<?php
/**
 * sms_settings.php — SMS Configuration now lives in Settings as a tab
 * (same design as the Facebook tab). Kept so old links/bookmarks still work.
 * Backend: ../backend/sms_config.php
 */
header('Location: settings.php?tab=sms');
exit;
